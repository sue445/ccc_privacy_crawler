development:
  adapter: sqlite3
  database: db/gemoire_development.db

postgresql: &postgresql
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("MAX_THREADS") { 5 } %>

test:
  <<: *postgresql
  database: ccc_privacy_crawler_test
  username: postgres
  password: postgres
  port: <%= ENV.fetch("POSTGRESQL_PORT") { 5432 } %>
  host: localhost

production:
  adapter: postgresql
  encoding: unicode
  pool: 5
  url: <%= ENV['DATABASE_URL'] %>
